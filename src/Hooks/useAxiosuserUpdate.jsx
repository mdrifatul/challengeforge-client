import axios from "axios";

const axiosUpdateUser = axios.create({
  baseURL: 'http://localhost:5000'
})

const useAxiosuserUpdate = () => {
  return (
    <div>
      
    </div>
  );
};

export default useAxiosuserUpdate;